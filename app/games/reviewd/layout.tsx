export const metadata = {
  title: "Reviewd",
  description: "Guess the movie from a scathing low-star Letterboxd review.",
};

export default function ReviewdLayout({ children }: { children: React.ReactNode }) {
  return children;
}
