export const metadata = {
  title: "Soundtrack Guesser",
  description: "Guess the movie, game, or TV show from its soundtrack.",
};

export default function SoundtrackLayout({ children }: { children: React.ReactNode }) {
  return children;
}
