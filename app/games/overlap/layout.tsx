export const metadata = {
  title: "Overlap",
  description:
    "A word puzzle: sort nine words into four categories of three — one hidden word belongs to all four.",
};

export default function OverlapLayout({ children }: { children: React.ReactNode }) {
  return children;
}
